import {StyleSheet, Text, View } from 'react-native';
import React from 'react';
import * as yup from 'yup';

export default function One() {
  // console.log('hey how are you');
  // const seqNum = 'abcdefghijklmnopqrstuvwxyz';
  let result = '';
  
//   function generatepass (character: string , passlength: number){
//   for (let i = 0; i < length; i++) {
//     const randnum = Math.round(Math.random() * character.length);
//     const passgen = seqNum.charAt(randnum);
//     npassgen += passgen;
//   }
//   return result;
// }
  
  // console.log(generatepass(talib,4));

  return (
    <View>
      <Text>One</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
